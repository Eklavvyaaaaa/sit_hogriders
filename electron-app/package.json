{
  "name": "secure-exam-monitor-electron",
  "version": "1.0.0",
  "description": "Secure Exam Monitor wrapper",
  "main": "main.js",
  "type": "commonjs",
  "scripts": {
    "start": "cross-env VITE_DEV_SERVER_URL=http://localhost:5173 concurrently \"cd client && npm run dev\" \"wait-on http://localhost:5173 && electron .\"",
    "build:react": "cd client && npm run build",
    "build:electron": "electron-builder",
    "build": "npm run build:react && npm run build:electron"
  },
  "dependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.7",
    "wait-on": "^8.0.2"
  },
  "devDependencies": {
    "electron": "^34.2.0",
    "electron-builder": "^25.1.8"
  }
}